server:
  port: 0
spring:
  datasource:
    url: jdbc:h2:mem:internal_ops_test_${random.uuid};MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
