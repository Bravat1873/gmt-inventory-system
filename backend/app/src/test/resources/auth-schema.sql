DROP TABLE IF EXISTS user_session;
DROP TABLE IF EXISTS sys_user;
CREATE TABLE sys_user (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, username VARCHAR(80) NOT NULL UNIQUE, password_hash VARCHAR(255) NOT NULL, display_name VARCHAR(100) NOT NULL, enabled BOOLEAN NOT NULL DEFAULT TRUE, role VARCHAR(20) NOT NULL DEFAULT 'USER');
CREATE TABLE user_session (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, user_id BIGINT NOT NULL, token_hash CHAR(64) NOT NULL UNIQUE, expires_at TIMESTAMP NOT NULL, revoked_at TIMESTAMP, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
INSERT INTO sys_user(username,password_hash,display_name,enabled,role) VALUES ('admin','123','管理员',TRUE,'ADMIN');
